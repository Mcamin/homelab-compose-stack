# flaresolverr runs inside the gluetun network namespace; its published port lives in the
# gluetun stack .env. Paths/ids come from the central .env (CONFIG_ROOT, MEDIA_ROOT,
# DOWNLOADS_ROOT, PUID, PGID, TZ).
