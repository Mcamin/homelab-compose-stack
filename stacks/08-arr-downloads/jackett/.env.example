# jackett runs inside the gluetun network namespace.
# Its published UI port is configured in the gluetun stack (.env: JACKETT_PORT).
# Per-service config/media/download paths come from the central .env
# (CONFIG_ROOT, MEDIA_ROOT, DOWNLOADS_ROOT, PUID, PGID, TZ).
