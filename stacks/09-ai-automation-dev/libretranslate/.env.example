LIBRETRANSLATE_DOMAIN=libretranslate.local.example.com
LIBRETRANSLATE_PORT=5000
# Only load these language models (smaller/faster). Comma-separated.
LT_LOAD_ONLY=en,fr,de
TRAEFIK_CERTRESOLVER=letsencrypt
