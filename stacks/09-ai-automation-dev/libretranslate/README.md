# LibreTranslate

Self-hosted translation API/UI. `LT_LOAD_ONLY` limits which language models load (faster
start, less RAM). For GPU acceleration switch the image to `:latest-cuda` and uncomment
the GPU block.
