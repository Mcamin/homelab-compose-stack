# n8n

Workflow automation (low-code). `N8N_ENCRYPTION_KEY` protects stored credentials — set it
once and keep it; changing it makes existing credentials unreadable. `N8N_HOST`/`WEBHOOK_URL`
must match the URL you reach it on (webhooks break otherwise).
