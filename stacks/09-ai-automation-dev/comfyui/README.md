# ComfyUI

Node-based Stable Diffusion / image-generation UI. **Practically GPU-only** — uncomment the
GPU block and install the nvidia-container-toolkit. Put checkpoints/LoRAs/VAEs under the
`models` volume. There's no canonical image; swap `COMFYUI_IMAGE` for your preferred build.
