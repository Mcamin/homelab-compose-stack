# Twingate Connector

Zero-trust remote access — exposes selected internal resources without opening ports.
No web UI: create a Connector in the Twingate admin console, copy its **Network name** and
the **access/refresh tokens** into `.env`. The connector dials out, so no inbound ports.
