TZ=Europe/Berlin
PUID=1000
PGID=1000

CONFIG_ROOT=/path/to/config
DATA_ROOT=/path/to/data

SHARED_NETWORK=homelab_shared
PROXY_NETWORK=homelab_proxy

APP_DOMAIN=app.example.local
TRAEFIK_CERTRESOLVER=letsencrypt

# Optional for scoped Watchtower deployments
WATCHTOWER_SCOPE=homelab